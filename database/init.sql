CREATE DATABASE IF NOT EXISTS mesin_cuci_store;
USE mesin_cuci_store;
-- Grant privileges (Adjust user/host as needed in production)
-- GRANT ALL PRIVILEGES ON mesin_cuci_store.* TO 'appuser'@'%';
-- FLUSH PRIVILEGES;
-- Tables will be automatically created by Sequelize on application startup
-- because we have sync() enabled (or migrations).